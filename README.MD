# Bowling RAG API

## Описание

Этот проект реализует RAG (Retrieval-Augmented Generation) систему для поиска и выдачи информации о боулинге. Интегрирован с GigaChat для генерации ответов и Telegram ботом для взаимодействия с пользователями. Использует векторную базу Qdrant для поиска и Google Sheets для хранения данных.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r app/requirements.txt
```

### 2. Настройка переменных окружения

```bash
# Скопируйте шаблон
cp .env.example app/.env

# Отредактируйте app/.env с вашими настройками
nano app/.env  # или notepad app/.env
```

**Обязательные переменные в app/.env:**
```bash
# GigaChat
GIGACHAT_AUTH_KEY=ваш_ключ_гигачат
GIGACHAT_ACCESS_TOKEN=ваш_токен

# Qdrant Cloud
QDRANT_URL=https://ваш-qdrant-url:6333
QDRANT_API_KEY=ваш_qdrant_ключ

# Google Sheets
SERVICE_ACCOUNT_FILE=app/service_account.json
SPREADSHEET_ID=ваш_google_sheets_id

# Telegram Bot
TELEGRAM_TOKEN=ваш_telegram_token
SEARCH_API_URL=http://localhost:8000/search
```

### 3. Импорт данных

```bash
# Загрузите данные из Google Sheets в Qdrant
python app/ingest_sheets.py
```

### 4. Запуск API сервера

```bash
# Из корневой директории проекта
python app/main.py
```

Сервер запустится на `http://localhost:8000`

### 5. Запуск Telegram бота (опционально)

```bash
# В отдельном терминале
python app/telegram_bot.py
```

## 📋 API Endpoints

- `GET /health` - Проверка работоспособности
- `GET /ready` - Проверка готовности сервисов
- `POST /chat` - Прямое общение с GigaChat
- `POST /rag-answer` - RAG ответ для бота
- `POST /rag-answer-func` - RAG с function-calling
- `GET /docs` - Документация API (Swagger UI)

## 🏗️ Структура проекта

```
bowling-rag/
├── app/                    # Основной код приложения
│   ├── main.py            # FastAPI сервер
│   ├── config.py          # Конфигурация
│   ├── qdrant.py          # Работа с Qdrant
│   ├── ingest_sheets.py   # Импорт данных из Google Sheets
│   ├── load_data.py       # Альтернативный импорт
│   ├── telegram_bot.py    # Telegram бот
│   ├── requirements.txt   # Зависимости Python
│   ├── .env              # Переменные окружения (создать)
│   └── service_account.json # Ключ Google (добавить)
├── .env.example          # Шаблон переменных окружения
├── .gitignore           # Исключаемые файлы
├── README.MD            # Эта документация
└── .github/             # CI/CD workflows
```

## 🔧 Настройка сервисов

### Google Sheets API
1. Создайте проект в Google Cloud Console
2. Включите Google Sheets API
3. Создайте сервисный аккаунт
4. Скачайте JSON-ключ и поместите в `app/service_account.json`
5. Предоставьте доступ к таблице для сервисного аккаунта

### Qdrant Cloud
1. Зарегистрируйтесь на [Qdrant Cloud](https://cloud.qdrant.io/)
2. Создайте кластер
3. Получите URL и API ключ
4. Добавьте их в `app/.env`

### GigaChat
1. Получите доступ к GigaChat API
2. Создайте приложение и получите ключи
3. Добавьте их в `app/.env`

### Telegram Bot
1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Создайте нового бота и получите токен
3. Добавьте токен в `app/.env`

## 🚀 Продакшен развертывание

### На сервере:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/vezovelosumibe89151/brooklyn-bowl-rag.git
cd brooklyn-bowl-rag

# 2. Настройте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate
pip install -r app/requirements.txt

# 3. Настройте переменные окружения
cp .env.example app/.env
nano app/.env  # заполните своими ключами

# 4. Импортируйте данные
python app/ingest_sheets.py

# 5. Запустите сервер
python app/main.py
```

### Запуск в фоне:

```bash
# С nohup
nohup python app/main.py &

# Или с screen
screen -S bowling-api
python app/main.py
# Ctrl+A, D - отсоединиться
```

## 🔍 Отладка и тестирование

### Проверка подключений:

```bash
# Qdrant
python app/qdrant.py

# Google Sheets
python -c "from app.config import *; print('OK')"
```

### Просмотр логов:

```bash
# При запуске с --reload логи выводятся в консоль
python app/main.py
```

### Health check:

```bash
curl http://localhost:8000/health
curl http://localhost:8000/ready
```

## 📊 Мониторинг

- **Health Check**: `GET /health` - проверка жизнеспособности
- **Readiness Check**: `GET /ready` - проверка готовности всех сервисов
- **API Docs**: `GET /docs` - интерактивная документация

## 🔒 Безопасность

- Никогда не коммитьте реальные ключи в репозиторий
- Используйте `.env` файл для локальной разработки
- На сервере используйте переменные окружения системы
- Регулярно обновляйте зависимости

## 🐛 Устранение проблем

### "Module not found"
```bash
pip install -r app/requirements.txt
```

### "Connection refused" (Qdrant)
- Проверьте QDRANT_URL в app/.env
- Убедитесь, что Qdrant сервер запущен

### "Access denied" (Google Sheets)
- Проверьте service_account.json
- Убедитесь, что сервисный аккаунт имеет доступ к таблице

### "Invalid token" (Telegram)
- Проверьте TELEGRAM_TOKEN в app/.env
- Убедитесь, что бот не заблокирован

## 📝 Лицензия

Этот проект предназначен для образовательных и коммерческих целей.

## 🤝 Поддержка

Если возникли вопросы или проблемы:
1. Проверьте логи приложения
2. Убедитесь, что все сервисы настроены правильно
3. Проверьте документацию используемых API